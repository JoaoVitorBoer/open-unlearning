# @package _global_

defaults:
  - _self_
  - model: Llama-3.2-3B-Instruct
  - eval: tofu
  - paths: default
  - hydra: eval
  - experiment: null
  - quantization
  - adapter: null

model:
  model_args:
    device_map: cuda
  # Default to the standard HF causal LM loader; override this (e.g. with LoRAModelForEvaluation)
  # when evaluating merged adapters.
  model_handler: AutoModelForCausalLM

mode: eval
task_name: ???
seed: 0
